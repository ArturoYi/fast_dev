name: fast_dev_example
description: "用来手工试用 fast_dev 的最小 Flutter 应用。"
publish_to: none
version: 1.0.0+1

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  build_runner: ^2.7.2
  fast_dev:
    path: ../
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/
