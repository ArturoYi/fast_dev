---
title: 计划
outline: [2, 3]
---

# 计划

开发期工具。现在做 assets 和 fonts。预留键可以先写，这版会告警，不会当成已经实现。

## 阶段

| 阶段 | 状态 | 内容 |
| --- | --- | --- |
| **v0.0.x** | 进行中 | assets / fonts 生成；CLI（`config` / `gen`）；`build_runner`（同一个 `fast_dev` 包）；文档站 |
| **v0.1** | 计划 | 稳定现有 API；补测试与示例 |
| **v0.2** | 计划 | colors 生成器 |
| **v0.3** | 计划 | `fast_dev imports` |
| **更后面** | 想想 | flavors、transformers |

如果希望一些业务相关的能力，可以用 [Fast Package](https://github.com/ArturoYi/fast_package)。
