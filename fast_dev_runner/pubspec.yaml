name: fast_dev_runner
description: "Fast Dev 的 build_runner Builder：与 CLI 共用同一套生成计划。"
version: 0.0.1
publish_to: none

environment:
  sdk: ^3.12.2

dependencies:
  build: ^2.4.0
  fast_dev:
    path: ../
  glob: ^2.1.3
  package_config: ^2.1.0
  path: ^1.9.1

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.0
